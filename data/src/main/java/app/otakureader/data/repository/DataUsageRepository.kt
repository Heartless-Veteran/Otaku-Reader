package app.otakureader.data.repository

// DataUsageRepository interface is defined in :domain so that feature modules can use it
// without a direct dependency on :data.
// See: app.otakureader.domain.repository.DataUsageRepository
